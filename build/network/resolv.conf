# This is a manually written file; we will change this to the default systemd-resolved implementation as soon as we migrated the gateways (by enabling conditional DNS forwarding).
nameserver 10.1.4.4
nameserver 10.1.4.5
search intranet.digitec